[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "congruents"
version = "0.1.0"
description = "Python interface to CONGRUENTS C physics"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]

